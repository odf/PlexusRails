- title "Project <em>#{h @project.name}</em>".html_safe

%p
  %strong Organization:
  = @project.organization
%p
  %strong Members:
%ul
  - for membership in @project.memberships
    %li== #{membership.user.name} (#{membership.role})

= link_to 'Edit', edit_project_path(@project)
