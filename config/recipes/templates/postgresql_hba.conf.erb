# Let the admin account in locally without a password
local all postgres ident

# Authentication for Rails connectiong to the application's database
host <%= postgresql_database %> <%= postgresql_user %> ::1/128 md5
