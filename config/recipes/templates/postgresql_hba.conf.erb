# Let the admin account in locally without a password
local all postgres ident

# Rails, on the other hand, seems to insist on using passwords
local <%= postgresql_database %> <%= postgresql_user %> md5
